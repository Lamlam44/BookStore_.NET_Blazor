@page "/admin/invoices/edit/{Id}"
@layout AdminLayout
@inject StoreManagement.Client.Services.ApiService ApiService
@inject StoreManagement.Client.Services.IStoreService StoreService
@inject IJSRuntime JSRuntime
@inject NavigationManager Navigation

<h3 class="mb-4">Chức năng sửa hóa đơn đã được tắt</h3>

@* Trang này đã bị vô hiệu hóa chức năng sửa *@
<div class="alert alert-warning">Chức năng sửa hóa đơn đã được tắt.</div>
<button class="btn btn-secondary" @onclick='() => Navigation.NavigateTo("/admin/invoices")'>Quay lại Danh sách Hóa đơn</button>

@code {
    [Parameter]
    public string Id { get; set; } = string.Empty;

    private string errorMessage = string.Empty;

    protected override Task OnInitializedAsync() => Task.CompletedTask;

    private Task LoadInvoiceForEdit() => Task.CompletedTask;

    private void AddDetail() { }

    private void RemoveDetail(AdminInvoiceDetailItem item) { }

    private async Task HandleValidSubmit() { await Task.CompletedTask; }

    private string GetProductTitle(string? id) => id ?? string.Empty;

    private async Task OnProductSearch(ChangeEventArgs e) { await Task.CompletedTask; }

    private void AddProductFromSuggestion(Book product) { }

    private async Task ApplyVoucher() { await Task.CompletedTask; }
}
